mailWebSite
===========

find and mark mail address with &lt;a&gt; tag, when click goto the mail website.


### Usage:

```javascript
$('#content').mailWebSite();
```

### Options:

```javascript
$('#content').mailWebSite({
	aTarget : '_blank',
    aClass: 'myMailClass'
});
```